/**
 * Contrôleur de la page accounts.
 */
applicationModule.controller("AccountController", ["$scope", "$resource", "$http", "$httpParamSerializer", "$cookies",
    function($scope, $resource, $http, $httpParamSerializer, $cookies) {
        
    }
]);